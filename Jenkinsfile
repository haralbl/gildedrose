#!/usr/bin/env groovy

node {
    stage ('Preparation') {
        echo 'Cloning repo...'
        sh "git clone git@github.com:haralbl/gildedrose.git"
    }
    stage ('Build') {
        echo 'Hello World'
    }
    stage ('Results') {
        echo 'Hello World'
    }
}
